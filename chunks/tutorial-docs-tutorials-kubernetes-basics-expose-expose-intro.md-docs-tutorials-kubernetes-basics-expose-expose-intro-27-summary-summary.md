---
doc_id: tutorial/docs-tutorials-kubernetes-basics-expose-expose-intro.md/docs-tutorials-kubernetes-basics-expose-expose-intro
chunk_id: tutorial/docs-tutorials-kubernetes-basics-expose-expose-intro.md/docs-tutorials-kubernetes-basics-expose-expose-intro#27-summary
chunk_level: summary
chunk_type: prose
heading: Services and Labels
token_count: 49
summary: We see here that the label is attached now to our Pod. And we can query now the list of pods using the new label: ``` `kubectl get pods -l version=v1 ` ``` And we see the Pod.
---

We see here that the label is attached now to our Pod. And we can query now the
list of pods using the new label:
```
`kubectl get pods -l version=v1
`
```
And we see the Pod.