$schema: "https://moonrepo.dev/schemas/workspace.json"

# Project locations
projects:
  globs:
    - "doc_transformer"
    - "contextual-chunker"
    - "llms-txt-parser"

# VCS configuration
vcs:
  manager: "git"
  defaultBranch: "main"

# Version constraint
versionConstraint: ">=1.20.0"
